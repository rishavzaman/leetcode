class Solution:
    def minPartitions(self, n: str) -> int:
        # m = 0
        # for i in str(n):
        #     m = max(m,int(i))
        # return m

        # return max([int(d) for d in str(n)])

        return int(max(n))